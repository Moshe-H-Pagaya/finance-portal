{
  "name": "finance-portal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Pagaya Finance AI & Automation Portal — Cloud Run port of the Google Apps Script web app",
  "scripts": {
    "dev": "tsx watch server.ts",
    "build": "esbuild server.ts --platform=node --packages=external --bundle --format=cjs --outfile=dist/server.cjs && mkdir -p dist/public && cp -R public/* dist/public/",
    "start": "node dist/server.cjs",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.19.0",
    "express": "^4.21.2",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "esbuild": "^0.27.4",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2"
  },
  "engines": {
    "node": ">=22"
  }
}
